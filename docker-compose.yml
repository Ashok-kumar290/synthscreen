services:
  biolens:
    build: .
    ports:
      - "8501:8501"
    environment:
      BIOLENS_MODE: ${BIOLENS_MODE:-mock}
      BIOLENS_DB_PATH: /app/data/biolens.db
    volumes:
      - biolens_data:/app/data

volumes:
  biolens_data:
